version: '3'
services:
  frontend:
    container_name: front
    build:
      dockerfile: ./frontend/Dockerfile
      context: .
    ports:
      - "3000:3000"
    volumes:
      - type: bind
        source: ./frontend
        target: /app
      - type: volume
        source: node_modules
        target: /app/node_modules
    command: npm run dev
  
  backend:
    container_name: back
    build:
      dockerfile: ./backend/Dockerfile
      context: .
    volumes:
      - type: bind
        source: ./backend
        target: /app
      - ./backend/pyproject.toml:/app/pyproject.toml  # Mount the specific file
    working_dir: /app/api
    environment:
      - PYTHONPATH=/app/api
    ports:
      - 8000:8000
  
  db:
    container_name: db
    image: mysql:8.0
    platform: linux/x86_64  # Only required for M1 Mac
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: 'data'
      TZ: 'Asia/Tokyo'
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - 33306:3306

volumes:
  mysql_data:
  node_modules:
